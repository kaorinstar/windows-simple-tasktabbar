# セキュリティに関する方針

[English version](SECURITY.md)

この文書は [SECURITY.md](SECURITY.md) の日本語訳です。英語版と同じコミットで更新します。

## 脆弱性の報告方法

GitHub の非公開フォームから報告してください。

https://github.com/kaorinstar/windows-simple-tasktabbar/security/advisories/new

通常のIssueは使わないでください。Issueは書いた時点で誰でも読める状態になり、取り消せません。

報告には、何が問題か、それによって何ができてしまうか、再現手順の3点を書いていただけると
助かります。Windows のバージョンと本アプリのバージョンも参考になります。本アプリの
バージョンは `WindowsSimpleTaskTabBar.exe` のファイルのプロパティで確認できます。

本プロジェクトの管理者は1名のため、返信は即時ではありません。7営業日以内を目安にしてください。
7営業日を過ぎても返信がない場合は、却下ではなく見落としです。アドバイザリーにコメントを追加して
再度お知らせください。

## 対象とするバージョン

最新のリリースのみです。本アプリは同梱ファイルのない実行ファイル1つで、修正はファイルの
置き換えとして届きます。公開済みのリリースに修正を当てることはしません。リリースの一覧は
https://github.com/kaorinstar/windows-simple-tasktabbar/releases にあります。

## 本アプリの動作範囲

報告の判断基準になるため、ソースを読まなくても分かるようここに書き出します。

- 通常のユーザー権限で動きます。管理者への昇格を求めず、サービスもタスクスケジューラの登録も
  作りません。
- **ネットワーク通信は一切行いません。** 送信も受信もないため、利用状況の送信、更新確認、
  アカウントのいずれもありません。
- 起動中のウィンドウの一覧、そのタイトル、各ウィンドウの実行ファイル名を Windows API から
  読み取ります。
- 書き込むファイルは1つだけです。`%APPDATA%\WindowsSimpleTaskTabBar\` の `settings.json` で、
  バーの高さ、配色と言語の設定、設定画面で作ったタブのグループを保存します。
- レジストリは1つの値だけを読みます。Windows のライトとダークの設定に追従するための、
  `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize` の
  `AppsUseLightTheme` です。レジストリへの書き込みはありません。
- 利用者がクリックしたときに、ウィンドウを前面に出す、最小化する、閉じるのいずれかを行います。
  本アプリがウィンドウに対してできることは、すべて Windows のタスクバーでもできることです。

## すでに把握している事項

以下は文書化済みです。これらの報告からは、すでに書かれている内容以上のものは得られません。

- **実行ファイルに署名していません。** そのため初回起動時に SmartScreen の警告が出ます。
  証明書の購入が解決策ですが、購入していません
  （[#71](https://github.com/kaorinstar/windows-simple-tasktabbar/issues/71)）。
- **3段階の前面化処理は、Windows の前面化制限を迂回します。** 使っているのは公開されたAPI
  （`SetForegroundWindow`、`AttachThreadInput`、`keybd_event` による Alt キーの押下と解放）
  だけで、呼び出すのは利用者自身が起動したプロセスです。これによって権限が増えることは
  ありません。手が届かなかったウィンドウも、同じ利用者がすでに所有しているウィンドウです。
  処理の流れは [docs/architecture.ja.md](docs/architecture.ja.md) に書いています。
- **管理者権限で動くアプリのウィンドウは、完全には操作できません。** Windows の整合性レベルが
  通常のプロセスからの操作を止めるためです。これは制限が正しく働いている状態であり、
  迂回できる不備ではありません。
