# 変更履歴

新しい版が上に並びます。各項目は、マージされたプルリクエストの一覧ではなく、
利用者にとって何が変わったかを書きます。

リリースの説明文は、タグと一致する見出しの節から取り出します。項目は変更のたびに
`## Unreleased` の節へ書き足し、リリースを準備するときに見出しをバージョン番号へ書き換えます。
`Unreleased` に一致するタグはないため、書き足したままタグを付けるとワークフローが失敗します。
中身の空のリリースが公開されることはありません。手順は [README.ja.md](README.ja.md) の
「リリース手順」を参照してください。

この文書は [version.md](version.md) の日本語訳です。英語版と同じコミットで更新します。

## Unreleased

- 配色を設定で選べるようになりました。Windows に従う、常に明るい、常に暗い、の3つです。
  初期状態は Windows に従うため、選び直さないかぎり動作は変わりません。明るい・暗いを
  選んだ場合は、Windows 側が切り替わってもそのままです。時刻で自動的に切り替わる設定でも
  変わりません。
- 色を自動にした2つのグループに、同じ色が付かないようにしました。色は8つあり、これまでは
  他のグループの色を見ずに、名前から決まる色をそのまま使っていました。そのため3グループで
  約3分の1の確率で色が重複していました。手動で選んだ色を先に確保し、自動のグループは名前から
  決まる色が埋まっている場合に次の空き色へ移ります。9グループ以上では重複を避けられません。
- バー上で隣り合うグループにも、同じ色が付かないようにしました。設定でグループ化していない
  アプリ単位のグループが対象です。これらは設定ファイルに存在しないため、設定側では色を
  振り分けられませんでした。フォルダーのウィンドウが複数並び、その隣に別のアプリが複数並ぶと、
  同じ色になって1つのグループに見えていました。手動で選んだ色はそのまま残し、隣のグループが
  色を変えます。間に別のグループがある場合は、同じ色のままにします。
- 設定画面の色の一覧に、色名と色見本を表示するようにしました。これまでの「Colour 1」から
  「Colour 8」という表示を置き換えます。
- 設定画面のアプリ一覧が、チェックを付けるたびに先頭へ戻る問題を修正しました。スクロール位置は
  そのまま保たれます。

## v0.3.0

- タブをアプリ単位でまとめる設定を追加しました。同じアプリのウィンドウが隣り合って並び、
  タブの上端に共通の色が付きます。グループの境目には区切り線が入ります。新しく開いた
  ウィンドウは、行の末尾ではなく同じアプリの隣に入ります。設定画面で複数のアプリを1つの
  グループにまとめ、名前と色を指定することもできます。ブラウザーとエディターを1つの
  グループとして扱う、といった使い方ができます。初期状態では無効で、無効のときの動作は
  これまでと同じです。
- グループ化が有効なとき、タブのドラッグは同じグループの中では1枚だけを動かし、隣のグループの
  向こうまで引くとグループごと動かします。行の並び順はこれまでどおり自由に変えられます。
  ウィンドウが1つだけのアプリは、単体で移動します。
- 実行ファイルは77,312バイトです。v0.2.0 は60,416バイトでした。

## v0.2.0

- バーを閉じたとき、保持していたものをすべて解放するようにしました。閉じ方によらず通知領域の
  アイコンは即座に消え、残骸が残りません。
- Windows のライト・ダークの設定変更に、その場で追従するようにしました。時刻で自動的に
  切り替わる設定にも対応します。これまでは起動時に読んだ配色を、再起動するまで使い続けて
  いました。
- 実行ファイルは60,416バイトです。v0.1.0 は59,904バイトでした。

## v0.1.0

最初のリリースです。

- Windows のタスクバーの上に細いバーを表示し、開いているウィンドウをタブとして並べます。
  タブを押すとそのウィンドウが前面に出ます。最小化されている場合は元に戻します。
  選択中のタブを押すと最小化します。Windows のタスクバーと同じ動きです。
- Windows は、前面にないアプリが自分を前面へ出すことを制限しています。3つの方法を順に試すため、
  通常の要求では拒否される場面でもウィンドウを切り替えられます。
- タブは × か中央ボタンのクリックで閉じられます。横にドラッグすると並び順を変えられます。
- タブを右クリックすると、閉じる・他のタブを閉じる・左側のタブを閉じる・右側のタブを閉じる・
  最小化のメニューが出ます。タブのない場所を右クリックすると、設定・再読み込み・終了の
  メニューが出ます。
- 通知領域（タスクトレイ）のアイコンにも同じメニューを用意しました。バーが隠れているときでも
  操作できます。
- タブにマウスを重ねると、ウィンドウの完全な題名を表示します。
- バーの高さは、設定画面で標準（34px）とコンパクト（24px）から選べます。選択は即座に反映され、
  `%APPDATA%\WindowsSimpleTaskTabBar\` に保存されます。
- タブが入りきらないときは、マウスホイールか両端の矢印で横に送れます。切り替えたウィンドウの
  位置へ自動で追従します。表示から外れるウィンドウはありません。
- Windows の配色設定（ライト・ダーク）に合わせて表示します。設定は起動時に読み取ります。
- 実行ファイルは59,904バイトの1つだけで、同時に入れるものはありません。Windows 10 バージョン1903
  以降と Windows 11 に標準で入っている .NET Framework 4.8 向けに作っており、AnyCPU のため
  ARM版のWindowsでも動きます。
